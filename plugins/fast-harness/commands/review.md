---
description: Review the current change with the default cross-model review path
---

Use the `cross-review` skill. This alias makes cross-model review the default
for `/review`; preserve the skill's peer auto-selection and fallback behavior.
