---
description: Run the full recoverable Matt-based harness workflow
---

Use the `fast-harness-full` skill with `$ARGUMENTS`. This command always selects
full mode; arguments describe the goal and cannot switch it to quick mode.
