---
description: Run real-browser E2E through a Trace Browser fingerprint profile
---

Use the `e2e-browser` skill with `$ARGUMENTS`; it owns browser selection,
connection, execution, and evidence capture.
