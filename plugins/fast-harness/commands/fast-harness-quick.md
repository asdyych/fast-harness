---
description: Run the quick recoverable Matt-based harness workflow
---

Use the `fast-harness-quick` skill with `$ARGUMENTS`. This command always selects
quick mode; arguments describe the goal and cannot switch it to full mode.
