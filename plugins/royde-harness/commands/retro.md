---
description: Retrospective to improve this harness plugin — find 3+ friction patterns, draft ready-to-paste plugin edits, apply on approval
---

Use the `retro` skill to improve the `royde-harness` plugin itself.

Look at where the harness helped or got in the way recently. For each pattern
seen **3+ times** (a guard false-positive, a worktree-dev gap, a review-loop rule
that misfired, a repeated manual workflow with no skill), classify it (plugin
defect / usage gap / out-of-scope personal config) and draft a ready-to-paste
edit to the relevant plugin file. Show the proposals, apply only the ones the
user approves, then verify. If there is no real 3+ pattern, say so and stop —
don't manufacture findings.

$ARGUMENTS
